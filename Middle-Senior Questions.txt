Какие виды полиморфизма есть ? ad-hoc полиморфизм (виртуальные методы, перегрузка функций, generics) diff questions

Из чего состоит CLR ?

SOLID
L - заменяя класс все должно работать так же без сбоев

Jit-compier который компилирует (выполняет) IL код
Инкапсуляция и сокрытие ?

Boxing and Unboxing почему второе тяжелее ?

mumtable vs immutable strings
Что делает GC после освобождения - Дефрагментирует память

Пример - стена с дырками, после освобожения памяти он эти керпичик делает без дырок

Где бы я использовал многопоточность, а где асинхронность ?

Сколько потоков будет в пуле когда ты ранишь свое приложение ?

round tripping для процессоров

локально Azure function statefull потому что надо настроить энвайрмент.
Auzure Storage Emulator

Horeography (стучится друг к другу) и Орхестрация (через очередь например)

6 принцепов Реста:
1 не сохранять состояние

2 unifi interface

Get не менять состояние (не должен)

Сursor в SQL будем использовать только когда хотим построчно что то сделать с каждый элементом таблицы
анонимная функция function() { }
стрелочная функция () => {}

read commited - дефолтная настройка в SQL
на создание коннекшена уходит больше времени чем на записть данных в таблицу (можно посмотреть в профайлере)
в ажуре functions нету hot time